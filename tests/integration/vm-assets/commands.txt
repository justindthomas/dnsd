; VPP startup CLI — runs after plugins load, before main loop takes
; over. The dpdk plugin already created "wan" from the startup.conf
; `dpdk { dev 0000:00:04.0 { name wan } }` block, so we just bring
; it up and assign addresses here.

set interface state wan up
set interface ip address wan 10.99.0.2/24
set interface ip address wan 2001:db8:99::2/64
