; VPP startup exec-commands.
;
; The dpdk plugin has already created `wan` from startup.conf by
; the time this runs. We intentionally DON'T do interface config
; here — carrier state during VPP init is racy for virtio-dpdk and
; `set interface ip address` silently fails when the interface is
; operationally down. All interface config happens in
; configure-vpp.sh (ExecStartPost) where carrier is reliably up.
;
; This file is kept so existing systemd units / startup.conf that
; reference it don't error on missing path.

; (intentionally empty)
